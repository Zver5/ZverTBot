[Unit]
Description=VPS Stats HTTP Server
After=network.target

[Service]
Type=simple
User=root
Environment=PYTHONPATH={{INSTALL_DIR}}
ExecStart={{INSTALL_DIR}}/.venv/bin/python {{INSTALL_DIR}}/hass/stats/stats-http.py
Restart=always

[Install]
WantedBy=multi-user.target
